<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.FacilityInfoMapper">

    <select id="selectFacilityByApartmentId" resultType="com.atguigu.lease.model.entity.FacilityInfo">
        select id,
               name,
               type,
               icon
        from facility_info
        where is_deleted = 0
          and id in (select facility_id from apartment_facility where is_deleted = 0 and apartment_id = #{id})
    </select>
    <select id="selectFacilityInfoById" resultType="com.atguigu.lease.model.entity.FacilityInfo">
        select f.type,
               f.name,
               f.id,
               f.icon,
               r.room_id
        from facility_info f
                 left join room_facility r
                           on r.facility_id = f.id
        where
            f.is_deleted = 0 and
            r.is_deleted = 0 and room_id = #{id}
    </select>
</mapper>
