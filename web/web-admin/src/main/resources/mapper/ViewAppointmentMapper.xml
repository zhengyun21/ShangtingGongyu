<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.lease.web.admin.mapper.ViewAppointmentMapper">
    <resultMap id="AppointmentVo" type="com.atguigu.lease.web.admin.vo.appointment.AppointmentVo" autoMapping="true">
        <association property="apartmentInfo" javaType="com.atguigu.lease.model.entity.ApartmentInfo" autoMapping="true">
            <id column="apart_id" property="id"></id>
            <result column="apart_name" property="name"></result>
        </association>
    </resultMap>

    <select id="selectByIPage" resultMap="AppointmentVo">
        select
            va.id,
            va.user_id,
            va.apartment_id,
            va.name,
            va.phone,
            va.appointment_time,
            va.additional_info,
            va.appointment_status,
            ai.id apart_id,
            ai.province_id,
            ai.province_name,
            ai.city_name,
            ai.city_id,
            ai.district_name,
            ai.district_id,
            ai.address_detail,
            ai.name apart_name
        from
            view_appointment va
        left join apartment_info ai
        on  ai.is_deleted = 0 and
            va.apartment_id = ai.id
            <where>
                va.is_deleted = 0
                <if test="queryVo.provinceId != null">
                    and ai.province_id = #{queryVo.provinceId}
                </if>
                <if test="queryVo.cityId != null">
                    and ai.city_id = #{queryVo.cityId}
                </if>
                <if test="queryVo.districtId != null">
                    and ai.distinctId = #{queryVo.districtId}
                </if>
                <if test="queryVo.apartmentId != null">
                    and ai.id = #{queryVo.apartmentId}
                </if>
                <if test="queryVo.name != null and queryVo.name != '' ">
                    and va.name like concat('%', #{queryVo.name}, '%')
                </if>
                <if test="queryVo.phone != null and queryVo.phone != '' ">
                    and va.phone like concat('%', #{queryVo.phone}, '%')
                </if>
            </where>
    </select>
</mapper>
